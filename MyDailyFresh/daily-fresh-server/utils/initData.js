const model = require('../models/model');

const Product = model.product;
const Category = model.category;

const initProduct = (appkey) => {

};

const initCategory = (appkey) => {

};

module.exports = {
  initProduct,
  initCategory,
};


